!
router bgp 65111
no bgp default ipv4-unicast
bgp log-neighbor-changes
neighbor MPLS peer-group
neighbor MPLS remote-as 65111
neighbor MPLS update-source Loopback0
neighbor 10.0.0.32 peer-group MPLS
neighbor 10.1.1.1 remote-as 65001
  
address-family ipv4
no synchronization
neighbor MPLS send-community both
neighbor MPLS soft-reconfiguration inbound
no neighbor 10.0.0.32 activate
no auto-summary
exit-address-family

address-family ipv4 vrf VRFgreen
no synchronization
neighbor 10.1.1.1 remote-as 65001
neighbor 10.1.1.1 activate
neighbor 10.1.1.1 send-community both
neighbor 10.1.1.1 soft-reconfiguration inbound
exit-address-family

address-family ipv4 vrf VRFblue
no synchronization
redistribute ospf 1 vrf VRFblue match internal external 1 external 2
exit-address-family

address-family vpnv4
neighbor MPLS send-community both
neighbor 10.0.0.32 activate
exit-address-family
!
